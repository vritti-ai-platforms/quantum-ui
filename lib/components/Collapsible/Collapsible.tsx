import {
  Collapsible as ShadcnCollapsible,
  CollapsibleContent as ShadcnCollapsibleContent,
  CollapsibleTrigger as ShadcnCollapsibleTrigger,
} from '../../../shadcn/shadcnCollapsible';

// Collapsible root component for expandable/collapsible sections
export const Collapsible = ShadcnCollapsible;

// Trigger that toggles the collapsible open/closed state
export const CollapsibleTrigger = ShadcnCollapsibleTrigger;

// Content that shows/hides when the collapsible is toggled
export const CollapsibleContent = ShadcnCollapsibleContent;
